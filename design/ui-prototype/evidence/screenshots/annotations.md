# Annotated Screenshot Notes

> Status: Q17 screenshot annotation note. Image files are generated by
> `node --experimental-strip-types design/ui-prototype/scripts/visual-smoke.ts`.

| Screenshot | Annotation | Observations | Stage A items |
| :---- | :---- | :---- | :---- |
| `desktop-normal-shell.png` | Desktop normal shell: deck-primary three-rail layout, quiet chrome, left static slide rail, right Outline inspector, bottom controls, and persistent status bar. | Browser smoke checks the deck title is visible and reports horizontal overflow status in `../validation-smoke.json`. | Exact rail widths, persisted layout preferences, production component library, and acceptance criteria. |
| `desktop-diagnostics-state.png` | Desktop diagnostics state: top-center blocking bar, bottom health signal, read-only diagnostic locator/hint, and no repair/editor actions. | Smoke verifies the page is nonblank; visual review should confirm the blocking bar does not cover bottom controls or inspector details. | Final diagnostic taxonomy, copy, severity thresholds, and accessible routing behavior. |
| `desktop-provenance-state.png` | Desktop provenance state: dark mode, manifest selector, format capability chips, artifact inventory, and known-limitation sections. | Smoke verifies inspectability of the dark-mode state and records overflow status. | Player App data source, exact manifest/evidence fields, copy/open affordances, and raw JSON fold behavior. |
| `mobile-viewer-state.png` | Narrow/mobile viewer state: deck-first responsive viewer with sticky controls and folded inspector access, not a full mobile presenter promise. | Smoke checks a 390 x 844 viewport with rails folded by default and records horizontal overflow status. | Mobile drawer behavior, touch target audit, final fullscreen/touch mapping, and whether status bar persists on mobile. |
| `presenter-view-state.png` | Presenter-view representation: explicit presenter metadata flow with notes hidden from the normal player view and browser multi-screen limitations left for Stage A. | Recommended screenshot when cheap; not required to freeze presenter layout. | Browser Window Management research, desktop shell option, display/window API evidence, and presenter-controller handling. |
