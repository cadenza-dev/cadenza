# Annotated Screenshot Notes

> Status: Q17 screenshot annotation note. Image files are generated by
> `npm --prefix design/ui-prototype run validate`.

| Screenshot | Annotation | Observations | Stage A items |
| :---- | :---- | :---- | :---- |
| `desktop-normal-shell.png` | Desktop normal shell: deck-primary three-rail layout with clickable slide rail, Outline inspector, icon-only layout controls, direct sash boundaries, bottom action-anchor controls, and persistent status bar. | Smoke verifies visible deck title, no horizontal overflow, no clipped buttons, `next` action-anchor navigation, thumbnail navigation, and Outline navigation. | Exact rail width defaults, persisted layout preferences, production component library, and acceptance criteria. |
| `desktop-diagnostics-state.png` | Desktop diagnostics state: blocking message bar, health route into Diagnostics, read-only locator/hint cards, copy affordances, and no repair/editor/re-export actions. | Smoke verifies visible deck title, no horizontal overflow, no clipped buttons, and locator copy feedback. | Final diagnostic taxonomy, copy, severity thresholds, clipboard fallback, and accessible routing behavior. |
| `desktop-provenance-state.png` | Desktop provenance state: dark mode inspector with format capabilities, evidence files, selector provenance, artifact inventory, adaptive section scrolling, and Raw Details represented as a copy affordance. | Smoke verifies inspectability of the dark-mode state with no horizontal overflow, no clipped buttons, all inspector section bodies configured for auto overflow, no Raw Details `<pre>`, and `Copy to the Clipboard` feedback. | Player App data source, exact manifest/evidence fields, copy/open affordances, and final raw-detail disclosure behavior. |
| `desktop-right-inspector-collapsed.png` | Desktop right-inspector collapsed state: right-side activity bar remains on the right edge, inspector content is fully absent, and the collapsed panel is fixed to the icon rail width. | Smoke verifies the collapsed rail is visible on the right, `.inspector-pane` is absent, no horizontal overflow is present, and the measured rail width is 42px. | Final collapsed-rail width, focus return, persisted side placement, and keyboard toggle behavior. |
| `desktop-swapped-rails.png` | Desktop swapped rails: inspector is on the left, slide rail is on the right, while the bottom status bar remains fixed instead of mirroring the rail order. | Smoke verifies `data-health-side="right"`, visible deck title, no horizontal overflow, no clipped buttons, and rail widths tied to inspector/slide kind rather than physical side. | Whether side swap survives Stage A, persistence model, and final keyboard/focus restoration behavior. |
| `desktop-activity-tooltip-state.png` | Activity-bar tooltip: hover tooltip renders as a pointer-style floating bubble above rails, without being clipped by the activity bar or side-panel boundary. | Smoke verifies visible tooltip text, no horizontal overflow, and no clipped buttons. | Final tooltip primitive, focus/hover timing, portal ownership, and accessibility behavior. |
| `desktop-fullscreen-state.png` | Fullscreen state: nonessential app chrome is hidden, deck content remains visible, keyboard/pointer navigation stays available, and action-anchor navigation does not black out the slide. | Smoke verifies fullscreen deck visibility, `next` action-anchor navigation, slide-background letterboxing, darker edge-hit overlay, pointer-position context-menu open/left-click dismissal, no horizontal overflow, and no clipped buttons. | Browser fullscreen API, presenter-controller research, exact input priority, and reduced-motion acceptance. |
| `desktop-fullscreen-final-exit.png` | Fullscreen final-anchor exit: the right edge advances out of fullscreen at the last action anchor instead of trapping the user at the end. | Smoke verifies last-anchor right navigation exits fullscreen and returns to the app shell. | Whether production uses browser fullscreen API, route state, or a dedicated fullscreen shell. |
| `desktop-fullscreen-presenter-menu.png` | Fullscreen context-menu presenter entry: right-click menu is theme-aware, anchored to the pointer, closes on selection, and routes to the presenter-view representation. | Smoke verifies the menu action opens the presenter representation and leaves fullscreen. | Presenter route contract, multi-window behavior, and browser permission model. |
| `desktop-fullscreen-menu-open.png` | Fullscreen context menu open: right-click opens a theme-aware pointer-anchored menu with presenter, copy, navigation, and exit actions. | Smoke verifies the menu appears near the pointer and remains visible for screenshot evidence. | Final command list, menu keyboard behavior, and product copy. |
| `mobile-viewer-state.png` | Narrow/mobile viewer state: deck-first responsive viewer with sticky action controls and compact drawer buttons, not a full mobile presenter promise. | Smoke checks a 390 x 844 viewport with no horizontal overflow and no clipped buttons. | Mobile touch target audit, final drawer behavior, final fullscreen/touch mapping, and whether status bar persists on mobile. |
| `mobile-slides-drawer.png` | Mobile slide drawer: folded slide rail opens as a sheet and can navigate action anchors without horizontal overflow. | Smoke verifies drawer visibility, action-anchor navigation from a thumbnail, no horizontal overflow, and no clipped buttons. | Drawer close semantics, focus return, and final mobile rail persistence. |
| `mobile-inspector-drawer.png` | Mobile inspector drawer: folded inspector opens as a sheet with icon topic tabs and summary-first sections. | Smoke verifies drawer visibility, Diagnostics topic reachability, no horizontal overflow, and no clipped buttons. | Mobile inspector density, focus order, and final topic-tab accessibility. |
| `presenter-view-state.png` | Presenter-view representation: current deck, next action anchor, notes boundary, timer/progress, and browser multi-screen limitation posture. | Smoke verifies visible deck title, no horizontal overflow, and no clipped buttons; visual review confirms current deck no longer clips inside the presenter panel. | Browser Window Management research, desktop shell option, display/window API evidence, and presenter-controller handling. |
